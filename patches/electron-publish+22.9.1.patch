diff --git a/node_modules/electron-publish/out/gitHubPublisher.js b/node_modules/electron-publish/out/gitHubPublisher.js
index 75e5ca2..32b4198 100644
--- a/node_modules/electron-publish/out/gitHubPublisher.js
+++ b/node_modules/electron-publish/out/gitHubPublisher.js
@@ -215,8 +215,15 @@ class GitHubPublisher extends _publisher().HttpPublisher {
 
     for (const asset of assets) {
       if (asset.name === fileName) {
-        await this.githubRequest(`/repos/${this.info.owner}/${this.info.repo}/releases/assets/${asset.id}`, this.token, null, "DELETE");
-        return;
+      	try {
+        	await this.githubRequest(`/repos/${this.info.owner}/${this.info.repo}/releases/assets/${asset.id}`, this.token, null, "DELETE");
+	  	} catch (e) {
+	  		_builderUtil().log.warn({error: e}, 'got error');
+	  		if (e.statusCode === 204) {
+	  			_builderUtil().log.warn({releaseId: release.id}, 'file successfully deleted');
+	  			return;
+	  		}
+	  	}
       }
     }
 
@@ -247,14 +254,15 @@ class GitHubPublisher extends _publisher().HttpPublisher {
       hostname: parsedUrl.hostname,
       path: parsedUrl.path,
       method: "POST",
+      timeout: 60 * 1000,
       headers: {
         accept: "application/vnd.github.v3+json",
         "Content-Type": _mime().default.getType(fileName) || "application/octet-stream",
-        "Content-Length": dataLength
-      }
-    }, this.token), this.context.cancellationToken, requestProcessor).catch(e => {
+        "Content-Length": dataLength,
+      },
+    }, this.token), this.context.cancellationToken, requestProcessor).catch(async e => {
       if (e.statusCode === 422 && e.description != null && e.description.errors != null && e.description.errors[0].code === "already_exists") {
-        return this.overwriteArtifact(fileName, release).then(() => this.doUploadFile(attemptNumber, parsedUrl, fileName, dataLength, requestProcessor, release));
+        await this.overwriteArtifact(fileName, release);
       }
 
       if (attemptNumber > 3) {
@@ -262,6 +270,9 @@ class GitHubPublisher extends _publisher().HttpPublisher {
       } else {
         return new Promise((resolve, reject) => {
           const newAttemptNumber = attemptNumber + 1;
+          _builderUtil().log.warn({
+              attempt: newAttemptNumber,
+            }, "attempting again");
           setTimeout(() => {
             this.doUploadFile(newAttemptNumber, parsedUrl, fileName, dataLength, requestProcessor, release).then(resolve).catch(reject);
           }, newAttemptNumber * 2000);
@@ -328,7 +339,8 @@ class GitHubPublisher extends _publisher().HttpPublisher {
       path: this.info.host != null && this.info.host !== "github.com" ? `/api/v3${path.startsWith("/") ? path : `/${path}`}` : path,
       headers: {
         accept: "application/vnd.github.v3+json"
-      }
+      },
+      timeout: 60 * 1000
     }, token, method), this.context.cancellationToken, data));
   }
 
